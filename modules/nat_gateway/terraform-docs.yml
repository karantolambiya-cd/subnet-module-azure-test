formatter: markdown table

sections:
  show:
    - header
    - requirements
    - providers
    - inputs
    - outputs

output:
  file: README.md
  mode: inject

settings:
  indent: 2
  required: true
  sensitive: true